/**
 * One column width for the whole conversation.
 *
 * The thread and the box you type in have to be the same lane, or the eye sees
 * two columns that nearly line up — which reads as a mistake even when nobody
 * can say what is wrong. Everything down the middle of the app uses this.
 */
export const LANE = 'mx-auto w-full max-w-3xl px-4 sm:px-6'
